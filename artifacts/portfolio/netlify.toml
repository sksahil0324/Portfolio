[build]
  command   = "npm install && npm run build:netlify"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — send all paths to index.html so React Router / wouter handles them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
